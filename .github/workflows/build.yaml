name: Build and Publichs Applications

on:
  push:
    tags:
      - v*

jobs:
  build_app:
    uses: ./.github/workflows/_build.yaml@main # Note: needs to reference a branch
    strategy:
      matrix:
        app: [scanner]
    with:
      app: ${{ matrix.app }}
